name: CRAPI Security Scan with Nuclei

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Start crAPI
        run: |
          docker compose -f docker-compose.yml --compatibility up -d
          
      - name: Wait for crAPI to be ready
        run: |
          for i in {1..30}; do
            if curl -s http://localhost:8888/health > /dev/null; then
              echo "crAPI is ready!"
              exit 0
            fi
            echo "Waiting for crAPI... ($i/30)"
            sleep 5
          done
          echo "crAPI did not become ready in time"
          exit 1

      - name: Register test user
        id: register
        run: |
          curl -X POST http://localhost:8888/identity/api/auth/signup \
            -H "Content-Type: application/json" \
            -d '{"email": "ci-test@example.com", "password": "CITest123!"}' || true
          
      - name: Get authentication token
        id: auth
        run: |
          RESPONSE=$(curl -s -X POST http://localhost:8888/identity/api/auth/login \
            -H "Content-Type: application/json" \
            -d '{"email": "ci-test@example.com", "password": "CITest123!"}')
          TOKEN=$(echo $RESPONSE | jq -r '.token')
          echo "token=$TOKEN" >> $GITHUB_OUTPUT

      # ИСПРАВЛЕННЫЙ ШАГ: правильный синтаксис для nuclei-action v3
      - name: Run Nuclei scan
        uses: projectdiscovery/nuclei-action@v3
        with:
          args: |
            -u http://localhost:8888
            -H "Authorization: Bearer ${{ steps.auth.outputs.token }}"
            -severity high,critical
            -include-rr
            -j
            -o nuclei-report.json
          # Параметры output больше нет - он передается через -o в args

      - name: Upload scan report
        uses: actions/upload-artifact@v4
        with:
          name: nuclei-scan-report
          path: nuclei-report.json
